<!DOCTYPE html>
<html>
<head>
  <title>My Sidebar Page</title>
  <style>
    .sidebar {
      position: fixed;
      top: 0;
      right: -200px; /* 우측에 위치, 시작은 화면 밖에서 시작 */
      width: 200px;
      height: 100%;
      background-color: #f1f1f1;
      overflow-x: hidden;
      padding-top: 20px;
      transition: all 0.5s ease-out; /* transition 속성 추가 */
    }

    .top {
      height: 50px;
      background-color: #ccc;
    }

    .sidebar img {
      display: block;
      margin: 0 auto;
      transform: translateX(100%);
    }

    /* .open 클래스 추가시 right 값을 0으로 변경하여 오른쪽에서 왼쪽으로 나오도록 설정 */
    .sidebar.open {
      right: 0;
    }

    /* 토글 버튼 위치 수정 */
    .toggle-btn {
      position: absolute;
      top: 10px;
      right: 0px; /* 토글 버튼을 오른쪽 화면 안으로 이동 */
    }
  </style>
</head>
<body>
  <div class="top">
    <button class="toggle-btn">Toggle Sidebar</button>
  </div>

  <div class="sidebar">
    <img src="images/mainAlarmFalse.png" alt="mainAlarmFlase Image">
  </div>

  <script>
    const sidebar = document.querySelector('.sidebar');
    const toggleBtn = document.querySelector('.toggle-btn');

    toggleBtn.addEventListener('click', function() {
      sidebar.classList.toggle('open');
    });
  </script>
</body>
</html>
