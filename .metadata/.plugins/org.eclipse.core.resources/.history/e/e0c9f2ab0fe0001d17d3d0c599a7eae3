<!DOCTYPE html>
<html>
  <head>
  <meta charset="UTF-8">
    <style>
      #popup {
        position: fixed;
        top: 0;
        right: -316px;
        width: 316px;
        height: 981px;
        background-color: white;
        border-left: 1px solid gray;
        z-index: 9999;
        transition: right 0.5s ease-in-out;
      }
      #popup.open {
        right: 0;
      }
      .popup-content {
        height: 100%;
        overflow-y: scroll;
        padding: 20px;
      }
      .overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 9998;
        background-color: rgba(0, 0, 0, 0.5);
        display: none;
      }
      .overlay.active {
        display: block;
      }
      /* 스크롤 색상 변경을 위한 스타일 */
      ::-webkit-scrollbar {
        width: 10px;
      }
      ::-webkit-scrollbar-thumb {
        background-color: #999;
      }
    </style>
  </head>
  <body>
    <button id="notification-btn" onclick="togglePopup()">알림</button>
    <div id="popup" class="">
      <div class="popup-content">
        <h2>알림 내용</h2>
        <p>내용1</p>
        <p>내용2</p>
        <p>내용3</p>
        <p>내용4</p>
        <p>내용5</p>
        <p>내용6</p>
        <p>내용7</p>
        <p>내용8</p>
        <p>내용9</p>
        <p>내용10</p>
      </div>
      <button id="popup-close-btn" onclick="togglePopup()">닫기</button>
    </div>
    <div class="overlay" onclick="togglePopup()"></div>
    <script>
    function showNotificationCount(count) {
        var notificationCount = document.getElementById("notification-count");
        if (notificationCount) {
          notificationCount.innerText = count;
          notificationCount.style.display = "inline-block";
        }
      }
      function togglePopup() {
        var popup = document.getElementById("popup");
        var overlay = document.querySelector(".overlay");
        var isOpen = popup.classList.toggle("open");
        overlay.classList.toggle("active");
        // 스크롤 색상 변경
        var body = document.querySelector("body");
        body.style.backgroundColor = isOpen ? "#f4f4f4" : "";
        // body에 'overflow: hidden' 스타일 적용하여 스크롤을 없앰
        body.style.overflow = isOpen ? "hidden" : "";
        // 알림 개수 표시
        showNotificationCount(0); // 알림 개수 초기화
      }
    </script>
  </body>
</html>
