<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Modal Sidebar</title>
    <style>
      /* 사이드바 트리거 버튼 */
      .sidebar-toggle {
        position: absolute;
        top: 20px;
        right: 20px;
        font-size: 30px;
        cursor: pointer;
      }

      /* 사이드바 메뉴 */
      .sidebar {
        position: fixed;
        top: 0;
        left: -300px;
        width: 300px;
        height: 100%;
        background-color: #fff;
        z-index: 10000;
        transition: left 0.3s ease-in-out;
      }

      /* 전체 화면을 덮는 모달 */
      .modal {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        z-index: 9999;
        display: none;
      }

      /* 사이드바 열릴 때, 모달 활성화 */
      .sidebar--active {
        left: 0;
      }

      /* 팝업 영역을 제외한 나머지 영역 어둡게 만들기 */
      .body--darken {
        background-color: rgba(0, 0, 0, 0.5);
      }

    </style>
  </head>
  <body>
    <button class="sidebar-toggle" style="position: absolute; top: 50px; right: 20px;">&#9776;</button>
    <div class="sidebar">
      <!-- 사이드바 내용 -->
      <h2>Sidebar</h2>
      <p>This is a sidebar menu.</p>
    </div>

    <div class="modal"></div>

    <script>
      const sidebarToggle = document.querySelector('.sidebar-toggle');
      const sidebar = document.querySelector('.sidebar');
      const modal = document.querySelector('.modal');
      const body = document.querySelector('body');

      sidebarToggle.addEventListener('click', () => {
        sidebar.classList.toggle('sidebar--active');
        modal.style.display = 'block';
        body.classList.toggle('body--darken');
      });

      /* 팝업 영역 바깥을 클릭하면 팝업이 사라지게 하기 */
      modal.addEventListener('click', (event) => {
        if (event.target === modal) {
          sidebar.classList.remove('sidebar--active');
          modal.style.display = 'none';
          body.classList.remove('body--darken');
        }
      });
    </script>
  </body>
</html>
